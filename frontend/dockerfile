FROM nginx:alpine

# Copy all files from the build context (frontend folder)
COPY . /usr/share/nginx/html/

# Copy custom nginx config (must exist in the same folder)
COPY ./nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
